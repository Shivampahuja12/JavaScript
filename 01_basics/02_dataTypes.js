"use strict";    // treat all js code as newer version of js

// alert(3+3);     // this will give error as we are working in node, not in browser

// console.log(3+3);
// console.log("Shivam");


let name = "shivam";
/* 

DATATYPES IN JAVASCRIPT:

    • PRIMITIVE DATA TYPES: 

    NUMBER - 2^53 - 1;
    BIGINT      // IF VALUE EXCEEDS THE NUMBNER RANGE(2^53-1) 
    BOOLEAN - true/false;

    null - standalone value     // it is a representation of an empty value.
    eg . let name = null;  // this name variable contains null
    
    undefined       // value which is not defined
    eg. let age;    // this is an example of undefined behaviour

    symbol - to find the uniqueness when we are working on multiple things

    • NON PREMITIVE DATATYPES:
    object


    on checking the type of any datatype the typeof null is an object
    and typeof undefined is undefined.
*/
// console.log(typeof null);            // object
// console.log(typeof undefined);       // undefined
